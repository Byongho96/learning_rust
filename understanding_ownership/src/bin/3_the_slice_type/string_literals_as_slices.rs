fn main() {
    // it’s a slice pointing to that specific point of the binary
    // `&str` is an immutable reference. The reference can indicate stack memory as well as heap memory.
    let s = "Hello, world!";
}
